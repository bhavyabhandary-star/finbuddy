DATABASE_URL=postgresql://finbuddy:finbuddy_dev_password@localhost:5432/finbuddy_rag
GROQ_API_KEY=

# From https://console.twilio.com/ dashboard, after joining the WhatsApp
# Sandbox (Messaging -> Try it out -> Send a WhatsApp message). Used to
# verify X-Twilio-Signature on inbound webhook requests -- see
# whatsapp_webhook.py's docstring. Leave unset for local dev (validation is
# skipped with a loud warning); MUST be set on any real deployment.
TWILIO_AUTH_TOKEN=
